{
  "components": {
    "schemas": {
      "ImportRequest": {
        "example": {
          "period": "202601"
        },
        "properties": {
          "period": {
            "description": "Target period in YYYYMM. Empty string falls back to today.",
            "examples": [
              "202601"
            ],
            "pattern": "^\\d{6}$|^$",
            "title": "Period",
            "type": "string"
          }
        },
        "required": [
          "period"
        ],
        "title": "ImportRequest",
        "type": "object"
      }
    }
  },
  "info": {
    "title": "Networth API",
    "version": "0.1.0"
  },
  "openapi": "3.1.0"
}
