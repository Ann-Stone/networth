{
  "components": {
    "schemas": {
      "EstateTransactionDetailCreate": {
        "description": "Estate_Journal subset accepted by the composite endpoint.\n\n``excute_price`` is intentionally absent: the service copies\n``journal.spending`` into it verbatim (sign preserved — a tax/fee/outflow\njournal is negative, a rent/inflow is positive). There are no account\nfields to resolve: Estate_Journal stores only the estate link, type,\namount, date and memo.",
        "example": {
          "estate_excute_type": "tax",
          "estate_id": "EST-001",
          "excute_date": "20260115",
          "memo": "Property tax"
        },
        "properties": {
          "estate_excute_type": {
            "description": "tax / fee / insurance / fix / rent / deposit",
            "enum": [
              "tax",
              "fee",
              "insurance",
              "fix",
              "rent",
              "deposit"
            ],
            "examples": [
              "tax"
            ],
            "title": "Estate Excute Type",
            "type": "string"
          },
          "estate_id": {
            "description": "Existing Estate.estate_id this transaction belongs to.",
            "examples": [
              "EST-001"
            ],
            "title": "Estate Id",
            "type": "string"
          },
          "excute_date": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "YYYYMMDD; defaults to journal.spend_date when omitted.",
            "examples": [
              "20260115"
            ],
            "title": "Excute Date"
          },
          "memo": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Free-form memo; defaults to journal.note when omitted.",
            "examples": [
              "Property tax"
            ],
            "title": "Memo"
          }
        },
        "required": [
          "estate_id",
          "estate_excute_type"
        ],
        "title": "EstateTransactionDetailCreate",
        "type": "object"
      }
    }
  },
  "info": {
    "title": "Networth API",
    "version": "0.1.0"
  },
  "openapi": "3.1.0"
}
