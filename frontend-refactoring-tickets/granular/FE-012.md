# FE-012 — Create src/stores/otherAssets.ts

> Depends on: FE-007 (api/otherAssets.ts), FE-002 (types/models.ts)

## Sub-tasks

| # | Task | Target file | Signature / key content | Acceptance command |
|---|------|-------------|-------------------------|--------------------|
| 1 | Create `src/stores/otherAssets.ts`. Export `useOtherAssetsStore`. | `view/src/stores/otherAssets.ts` | `export const useOtherAssetsStore = defineStore('otherAssets', () => { ... })` | `test -f view/src/stores/otherAssets.ts && grep -q 'useOtherAssetsStore' view/src/stores/otherAssets.ts` |
| 2 | Add stocks state + fetch: `stocks: ref<StockAsset[]>([])`, `stocksLoading`, `fetchStocks()`. Also `stockDetails: ref<StockJournal[]>([])`, `stockDetailsLoading`, `fetchStockDetails(stockId)`. | `view/src/stores/otherAssets.ts` | Standard store pattern for both | `grep -q 'fetchStocks' view/src/stores/otherAssets.ts && grep -q 'fetchStockDetails' view/src/stores/otherAssets.ts` |
| 3 | Add estates state + fetch: `estates`, `estatesLoading`, `fetchEstates()`. `estateDetails`, `estateDetailsLoading`, `fetchEstateDetails(estateId)`. | `view/src/stores/otherAssets.ts` | Standard pattern | `grep -q 'fetchEstates' view/src/stores/otherAssets.ts` |
| 4 | Add insurances state + fetch: `insurances`, `insurancesLoading`, `fetchInsurances()`. `insuranceDetails`, `insuranceDetailsLoading`, `fetchInsuranceDetails(insuranceId)`. | `view/src/stores/otherAssets.ts` | Standard pattern | `grep -q 'fetchInsurances' view/src/stores/otherAssets.ts` |
| 5 | Add loans state + fetch: `loans`, `loansLoading`, `fetchLoans()`. `loanDetails`, `loanDetailsLoading`, `fetchLoanDetails(loanId)`. | `view/src/stores/otherAssets.ts` | Standard pattern | `grep -q 'fetchLoans' view/src/stores/otherAssets.ts` |
| 6 | Add otherAssets state + fetch: `otherAssets: ref<OtherAsset[]>([])`, `otherAssetsLoading`, `fetchOtherAssets()`. | `view/src/stores/otherAssets.ts` | Standard pattern | `grep -q 'fetchOtherAssets' view/src/stores/otherAssets.ts` |
| 7 | Add `activeTab: ref<string>('stocks')` — tracks which asset tab is active. | `view/src/stores/otherAssets.ts` | `const activeTab = ref<string>('stocks')` | `grep -q 'activeTab' view/src/stores/otherAssets.ts` |
| 8 | Return all state + functions. | `view/src/stores/otherAssets.ts` | `return { activeTab, stocks, stocksLoading, fetchStocks, ... }` | `grep -q 'return {' view/src/stores/otherAssets.ts` |
| 9 | Run type-check. | — | — | `cd view && npm run type-check 2>&1 \| tail -3` |

## Notes

- Detail arrays (`stockDetails`, `estateDetails`, etc.) are always for the currently-selected parent record. Views set the parent ID by calling `fetchXDetails(parentId)`.
- Do not import any other store.
