# FE-009 — Create src/stores/dashboard.ts

> Depends on: FE-004 (api/dashboard.ts), FE-002 (types/models.ts)

## Sub-tasks

| # | Task | Target file | Signature / key content | Acceptance command |
|---|------|-------------|-------------------------|--------------------|
| 1 | Create `src/stores/dashboard.ts`. Export `useDashboardStore`. | `view/src/stores/dashboard.ts` | `export const useDashboardStore = defineStore('dashboard', () => { ... })` | `test -f view/src/stores/dashboard.ts && grep -q 'useDashboardStore' view/src/stores/dashboard.ts` |
| 2 | Add summary state: `summary: ref<DashboardSummary \| null>(null)`, `summaryLoading`, `fetchSummary()`. | `view/src/stores/dashboard.ts` | Standard store pattern | `grep -q 'fetchSummary' view/src/stores/dashboard.ts` |
| 3 | Add alarms state: `alarms: ref<DashboardAlarm[]>([])`, `alarmsLoading`, `fetchAlarms()`. | `view/src/stores/dashboard.ts` | Standard store pattern | `grep -q 'fetchAlarms' view/src/stores/dashboard.ts` |
| 4 | Add targets state: `targets: ref<TargetSetting[]>([])`, `targetsLoading`, `fetchTargets()`. | `view/src/stores/dashboard.ts` | Standard store pattern | `grep -q 'fetchTargets' view/src/stores/dashboard.ts` |
| 5 | Add budget state: `budget: ref<DashboardBudget \| null>(null)`, `budgetLoading`, `fetchBudget()`. | `view/src/stores/dashboard.ts` | Standard store pattern | `grep -q 'fetchBudget' view/src/stores/dashboard.ts` |
| 6 | Add gifts state: `gifts: ref<DashboardGift[]>([])`, `giftsLoading`, `fetchGifts(year)`. | `view/src/stores/dashboard.ts` | `async function fetchGifts(year: number)` | `grep -q 'fetchGifts' view/src/stores/dashboard.ts` |
| 7 | Return all state + functions. | `view/src/stores/dashboard.ts` | `return { summary, summaryLoading, fetchSummary, ... }` | `grep -q 'return {' view/src/stores/dashboard.ts` |
| 8 | Run type-check. | — | — | `cd view && npm run type-check 2>&1 \| tail -3` |

## Notes

- `fetchAlarms()` in this store calls `getDashboardAlarms()` (from `api/dashboard.ts`), not `getAlarms()` from `api/setting.ts`. They hit different endpoints.
- Do not import any other store inside this store.
