# FE-024 — CashFlowView — settle month + stock price section

> Depends on: FE-021 (CashFlowView scaffold), FE-005 (cashFlow API)

## Sub-tasks

| # | Task | Target file | Signature / key content | Acceptance command |
|---|------|-------------|-------------------------|--------------------|
| 1 | Read `PUT /monthly-report/balance/{vesting_month}/settle`, `GET /monthly-report/stock-prices/{vesting_month}`, and `POST /monthly-report/stock-prices` in api-reference.md. | — | — | `grep -B2 -A 15 "balance.*settle" api/docs/api-reference.md \| head -20` |
| 2 | Add a "月結" (settle) section to `CashFlowView.vue`: a button "執行月結" that calls `settleMonth(store.selectedMonth)` with a loading state. Show success/error toast. | `view/src/views/monthly-report/CashFlowView.vue` | `const settling = ref(false)` + `async function handleSettle() { settling.value = true; try { await settleMonth(store.selectedMonth); ElMessage.success('月結完成') } finally { settling.value = false } }` | `grep -q 'settleMonth\|handleSettle' view/src/views/monthly-report/CashFlowView.vue` |
| 3 | Add stock price section: a table showing `store.stockPrices` for the current month. Fetch on component mount alongside journals. | `view/src/views/monthly-report/CashFlowView.vue` | `store.fetchStockPrices()` in `onMounted` + `<el-table :data="store.stockPrices">` | `grep -q 'fetchStockPrices\|stockPrices' view/src/views/monthly-report/CashFlowView.vue` |
| 4 | Add stock price upload: a file input (`<el-upload>` or hidden `<input type="file">`) that calls `uploadStockPrices(file)`. On success show toast and `store.fetchStockPrices()`. | `view/src/views/monthly-report/CashFlowView.vue` | `<el-upload :auto-upload="false" :on-change="handleStockPriceUpload">` or manual file input | `grep -q 'uploadStockPrices\|handleStockPriceUpload' view/src/views/monthly-report/CashFlowView.vue` |
| 5 | Run type-check. | — | — | `cd view && npm run type-check 2>&1 \| tail -3` |

## Notes

- "月結" should be guarded with a confirm dialog (`el-message-box`) since it's a destructive operation.
- After settle, refresh the journal list: call `store.fetchJournals()`.
- Stock price CSV format is defined by the backend; the upload button just sends the file to the API.
