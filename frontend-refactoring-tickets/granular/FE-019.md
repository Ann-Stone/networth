# FE-019 — DashboardView — targets CRUD panel

> Depends on: FE-017 (DashboardView scaffold), FE-009 (dashboard store), FE-004 (api/dashboard.ts)

## Sub-tasks

| # | Task | Target file | Signature / key content | Acceptance command |
|---|------|-------------|-------------------------|--------------------|
| 1 | Read `GET/POST/PUT/DELETE /dashboard/targets` response shapes in api-reference.md. | — | — | `grep -B2 -A 25 'GET /dashboard/targets' api/docs/api-reference.md \| head -30` |
| 2 | Add targets section to `DashboardView.vue`. Call `store.fetchTargets()` on mount. | `view/src/views/DashboardView.vue` | `store.fetchTargets()` in `onMounted` | `grep -q 'fetchTargets' view/src/views/DashboardView.vue` |
| 3 | Render targets as a card list with an "Add" button. Each item shows `setting_name` and `target_amount` (via `<MoneyDisplay>`). | `view/src/views/DashboardView.vue` | `v-for="t in store.targets"` card row with delete icon button | `grep -q 'store.targets' view/src/views/DashboardView.vue` |
| 4 | Add create/edit dialog: an `<el-dialog>` with a form (`setting_name` text input, `target_amount` number input). On submit: call `createTarget(data)` or `updateTarget(id, data)`, then `store.fetchTargets()`. | `view/src/views/DashboardView.vue` | `const targetDialogVisible = ref(false)` + form `ref` + submit handler | `grep -q 'targetDialogVisible' view/src/views/DashboardView.vue` |
| 5 | Add delete confirmation: `<el-popconfirm>` on the delete icon. On confirm: call `deleteTarget(id)` then `store.fetchTargets()`. | `view/src/views/DashboardView.vue` | `<el-popconfirm title="確定刪除?" @confirm="handleDeleteTarget(t.target_id)">` | `grep -q 'popconfirm\|handleDeleteTarget' view/src/views/DashboardView.vue` |
| 6 | Run type-check. | — | — | `cd view && npm run type-check 2>&1 \| tail -3` |

## Notes

- Import `createTarget`, `updateTarget`, `deleteTarget` from `@/api/dashboard.ts` directly in the view — do not add mutation methods to the store.
- `target_amount` should be a numeric input. Use `el-input-number` or `el-input` with `type="number"`.
- Show a toast (`ElMessage.success`) after successful create/update/delete.
