# FE-033 — OtherAssetsView — other-assets tab

> Depends on: FE-028 (tab shell), FE-007 (api/otherAssets.ts)

## Sub-tasks

| # | Task | Target file | Signature / key content | Acceptance command |
|---|------|-------------|-------------------------|--------------------|
| 1 | Read `GET /assets/other-assets` and `GET /assets/other-assets/items` response shapes in api-reference.md. Understand the difference between the two endpoints. | — | — | `grep -B2 -A 25 "GET /assets/other-assets" api/docs/api-reference.md \| head -30` |
| 2 | Implement 其他資產 tab: call `store.fetchOtherAssets()` on tab activation. Replace `<EmptyState>` with other-assets table. | `view/src/views/other-assets/OtherAssetsView.vue` | `<el-table :data="store.otherAssets">` | `grep -q 'fetchOtherAssets' view/src/views/other-assets/OtherAssetsView.vue` |
| 3 | Other-asset table columns: key fields from `OtherAsset` per api-reference.md. Add/edit/delete actions. | `view/src/views/other-assets/OtherAssetsView.vue` | `asset_id` or equivalent PK per spec | `grep -q 'otherAssets\|asset_id' view/src/views/other-assets/OtherAssetsView.vue` |
| 4 | Other-asset create/edit dialog: form with required fields. Submit: `createOtherAsset / updateOtherAsset` → `store.fetchOtherAssets()`. | `view/src/views/other-assets/OtherAssetsView.vue` | `const otherAssetDialogVisible = ref(false)` | `grep -q 'otherAssetDialogVisible\|createOtherAsset' view/src/views/other-assets/OtherAssetsView.vue` |
| 5 | Delete with `<el-popconfirm>`: `deleteOtherAsset(id)` → `store.fetchOtherAssets()`. | `view/src/views/other-assets/OtherAssetsView.vue` | `deleteOtherAsset` in confirm handler | `grep -q 'deleteOtherAsset' view/src/views/other-assets/OtherAssetsView.vue` |
| 6 | Run type-check. | — | — | `cd view && npm run type-check 2>&1 \| tail -3` |

## Notes

- `OtherAsset` has no nested detail/journal sub-resource (unlike stocks/estates/insurance/loans). The tab is simpler — no expand row needed.
- `GET /assets/other-assets/items` may return a flat denormalized list; use it if the view needs to display items without grouping.

**POST_FRONTEND_TODO.md §3 rows touched:**
`Other_Asset.asset_index` — verify whether this field appears in the `GET /assets/other-assets` API response.
- If rendered (e.g. shown in table or used for sorting): **Decision = `keep`**.
- If not shown in the UI: **Decision = `drop`** (field exists only as backend sort-order utility).
- Set `Verified at` to `view/src/views/other-assets/OtherAssetsView.vue` in the same commit.
