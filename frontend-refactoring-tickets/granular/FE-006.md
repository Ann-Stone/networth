# FE-006 — Create src/api/yearReport.ts

> Depends on: FE-001 (request.ts), FE-002 (types/models.ts)

## Sub-tasks

| # | Task | Target file | Signature / key content | Acceptance command |
|---|------|-------------|-------------------------|--------------------|
| 1 | Create `src/api/yearReport.ts`. Implement `getBalanceReport(params?)` → `GET /reports/balance`. | `view/src/api/yearReport.ts` | `export function getBalanceReport(params?: { year?: number }): Promise<BalanceReport>` | `test -f view/src/api/yearReport.ts && grep -q 'getBalanceReport' view/src/api/yearReport.ts` |
| 2 | Implement `getExpenditureReport(type, params?)` → `GET /reports/expenditure/{type}`. The `type` param selects between expenditure breakdown types (read api-reference.md for valid values). | `view/src/api/yearReport.ts` | `export function getExpenditureReport(type: string, params?: { year?: number }): Promise<ExpenditureReport>` | `grep -q 'getExpenditureReport' view/src/api/yearReport.ts` |
| 3 | Implement `getAssetsReport(params?)` → `GET /reports/assets`. | `view/src/api/yearReport.ts` | `export function getAssetsReport(params?: { year?: number }): Promise<AssetReport>` | `grep -q 'getAssetsReport' view/src/api/yearReport.ts` |
| 4 | Run type-check — must pass with zero errors. | — | — | `cd view && npm run type-check 2>&1 \| tail -3` |

## Notes

- Read `api/docs/api-reference.md` Reports section for all three response shapes and allowed query parameters.
- These are read-only endpoints (no mutations). All functions use `request.get(...)`.
- `ExpenditureReport` shape may vary by `type` parameter — use a union type or a flexible interface if needed; default to `unknown` for nested chart data if the shape is complex.
