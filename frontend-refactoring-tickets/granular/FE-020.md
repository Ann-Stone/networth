# FE-020 — DashboardView — budget overview + gifts

> Depends on: FE-017 (DashboardView scaffold), FE-009 (dashboard store), FE-016 (charts)

## Sub-tasks

| # | Task | Target file | Signature / key content | Acceptance command |
|---|------|-------------|-------------------------|--------------------|
| 1 | Read `GET /dashboard/budget` and `GET /dashboard/gifts/{year}` response shapes in api-reference.md. | — | — | `grep -B2 -A 25 'GET /dashboard/budget' api/docs/api-reference.md \| head -30` |
| 2 | Add budget overview section to `DashboardView.vue`. Call `store.fetchBudget()` on mount. Render a summary card showing current-month budget vs. actual spend by category. | `view/src/views/DashboardView.vue` | `store.fetchBudget()` + render using data from `store.budget` | `grep -q 'fetchBudget' view/src/views/DashboardView.vue` |
| 3 | If `DashboardBudget` data includes series suitable for a bar chart (budget vs actual per category), render a `<BarChart>` component. Otherwise render as a table. | `view/src/views/DashboardView.vue` | Conditional: if chart data available, `<BarChart :xData="..." :series="..." height="240px">`; else `<el-table>` | `grep -q 'BarChart\|el-table' view/src/views/DashboardView.vue` |
| 4 | Add gifts section: call `store.fetchGifts(currentYear)` on mount. Render a compact table showing gifts for the year. | `view/src/views/DashboardView.vue` | `const currentYear = new Date().getFullYear()` + `store.fetchGifts(currentYear)` | `grep -q 'fetchGifts' view/src/views/DashboardView.vue` |
| 5 | Run type-check. | — | — | `cd view && npm run type-check 2>&1 \| tail -3` |

## Notes

- After this ticket, DashboardView has 4 sections: summary row (FE-017), alarms (FE-018), targets (FE-019), budget+gifts (this ticket). The phase gate verifies all 4 are populated.
- "Gifts" in the API context are income records of a specific type — read api-reference.md `/dashboard/gifts/{year}` for exact shape.
- Use `<el-skeleton>` while `store.budgetLoading` or `store.giftsLoading` are true.
