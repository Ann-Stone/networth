# FE-001 — Patch request.ts — fix envelope field name, remove unused auth interceptor

> Depends on: (none)

## Sub-tasks

| # | Task | Target file | Signature / key content | Acceptance command |
|---|------|-------------|-------------------------|--------------------|
| 1 | Replace `res.message` with `res.msg` in the success branch of the response interceptor. | `view/src/utils/request.ts` | `ElMessage.error(res.msg \|\| '請求失敗')` and `return Promise.reject(new Error(res.msg \|\| 'Error'))` | `grep -q 'res\.msg' view/src/utils/request.ts && ! grep -q 'res\.message' view/src/utils/request.ts` |
| 2 | Remove the auth-token request interceptor (the `localStorage.getItem('token')` block). No auth is needed in this single-user app. Keep the interceptor registration but make the callback a simple pass-through. | `view/src/utils/request.ts` | `service.interceptors.request.use((config) => config, (error) => Promise.reject(error))` | `! grep -q 'localStorage' view/src/utils/request.ts` |
| 3 | Fix the error branch in the response interceptor: use `res.msg` (not `res.message`). Also handle the case where `error.response?.data` uses `msg` not `message`. | `view/src/utils/request.ts` | `const msg = error.response?.data?.msg \|\| error.message \|\| '網路錯誤'` | `grep -q "data?.msg" view/src/utils/request.ts` |
| 4 | Verify `ApiResponse` type is exported from `src/types/models.ts` and imported in `request.ts`. The interceptor signature must use it. | `view/src/utils/request.ts` | `import type { ApiResponse } from '@/types/models'` at top; `AxiosResponse<ApiResponse<unknown>>` in interceptor | `grep -q "ApiResponse" view/src/utils/request.ts` |
| 5 | Run type-check to confirm no TS errors in this file. | — | — | `cd view && npm run type-check 2>&1 \| grep -v "error TS" \| head -5; cd view && npm run type-check 2>&1 \| grep "Found 0 errors"` |

## Notes

- `ApiResponse<T>` must already exist in `src/types/models.ts` before type-check passes. If FE-002 has not run yet, add a temporary minimal definition to unblock type-check: `export interface ApiResponse<T> { status: number; data: T; msg: string }`.
- Do not introduce `message` anywhere; the backend envelope field is `msg`.
- Keep the `timeout: 10000` and `baseURL` settings unchanged.
