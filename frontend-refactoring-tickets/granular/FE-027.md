# FE-027 — AssetView (GET /reports/assets)

> Depends on: FE-011 (yearReport store), FE-016 (charts)

## Sub-tasks

| # | Task | Target file | Signature / key content | Acceptance command |
|---|------|-------------|-------------------------|--------------------|
| 1 | Read `GET /reports/assets` response shape in api-reference.md. | — | — | `grep -B2 -A 40 "GET /reports/assets" api/docs/api-reference.md \| head -45` |
| 2 | Replace `AssetView.vue` stub. Import `useYearReportStore`. Call `store.fetchAssetsReport()` on mount. | `view/src/views/year-report/AssetView.vue` | `const store = useYearReportStore(); onMounted(() => store.fetchAssetsReport(store.selectedYear))` | `grep -q 'fetchAssetsReport' view/src/views/year-report/AssetView.vue` |
| 3 | Add year selector bound to `store.selectedYear`. On change, re-fetch. | `view/src/views/year-report/AssetView.vue` | Same year picker pattern as FE-025/FE-026 | `grep -q 'selectedYear' view/src/views/year-report/AssetView.vue` |
| 4 | Render the asset overview. If the response includes asset category breakdown (stocks, estates, insurance, etc.), render a `<DonutChart>` or stacked `<BarChart>`. Also render a summary table. | `view/src/views/year-report/AssetView.vue` | Based on actual `AssetReport` shape | `grep -q 'DonutChart\|BarChart\|el-table' view/src/views/year-report/AssetView.vue` |
| 5 | Show `<el-skeleton>` while `store.assetsLoading`. | `view/src/views/year-report/AssetView.vue` | Standard loading pattern | `grep -q 'assetsLoading' view/src/views/year-report/AssetView.vue` |
| 6 | Run type-check. | — | — | `cd view && npm run type-check 2>&1 \| tail -3` |

## Notes

- After completing FE-025, FE-026, FE-027, all three year-report views should be working. The phase gate is: all three render with data from the API.
- Read the actual `AssetReport` shape carefully; it may contain nested data per asset category.
