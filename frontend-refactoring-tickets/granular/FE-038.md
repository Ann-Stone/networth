# FE-038 — RemindSettingView — alarms CRUD

> Depends on: FE-008 (setting store), FE-003 (api/setting.ts)

## Sub-tasks

| # | Task | Target file | Signature / key content | Acceptance command |
|---|------|-------------|-------------------------|--------------------|
| 1 | Read `GET/POST /settings/alarms`, `PUT/DELETE /settings/alarms/{alarm_id}` shapes in api-reference.md. | — | — | `grep -B2 -A 20 "GET /settings/alarms$" api/docs/api-reference.md \| head -25` |
| 2 | Replace `RemindSettingView.vue` stub. Import `useSettingStore`. Call `store.fetchAlarms()` on mount. | `view/src/views/setting/RemindSettingView.vue` | `const store = useSettingStore(); onMounted(() => store.fetchAlarms())` | `grep -q 'useSettingStore\|fetchAlarms' view/src/views/setting/RemindSettingView.vue` |
| 3 | Render alarms as `<el-table :data="store.alarms">`. Columns: `alarm_type`, `alarm_date`, `content`, `due_date`. Add/edit/delete actions. | `view/src/views/setting/RemindSettingView.vue` | Table with `v-loading="store.alarmsLoading"` | `grep -q 'store.alarms\|alarm_type' view/src/views/setting/RemindSettingView.vue` |
| 4 | Alarm create/edit dialog: form with `alarm_type` (text or select), `alarm_date` (date picker), `content` (textarea), `due_date` (date picker, optional). | `view/src/views/setting/RemindSettingView.vue` | `const alarmDialogVisible = ref(false)` + form | `grep -q 'alarmDialogVisible' view/src/views/setting/RemindSettingView.vue` |
| 5 | Submit: `createAlarm(data)` or `updateAlarm(id, data)` → `store.fetchAlarms()`. Close dialog on success. | `view/src/views/setting/RemindSettingView.vue` | `import { createAlarm, updateAlarm, deleteAlarm } from '@/api/setting'` | `grep -q 'createAlarm\|updateAlarm' view/src/views/setting/RemindSettingView.vue` |
| 6 | Delete with `<el-popconfirm>`: `deleteAlarm(alarmId)` → `store.fetchAlarms()`. | `view/src/views/setting/RemindSettingView.vue` | `<el-popconfirm @confirm="handleDeleteAlarm(row.alarm_id)">` | `grep -q 'deleteAlarm' view/src/views/setting/RemindSettingView.vue` |
| 7 | Run type-check. | — | — | `cd view && npm run type-check 2>&1 \| tail -3` |

## Notes

- `alarm_date` and `due_date` are `YYYYMMDD` strings in the API. Convert `Date → 'YYYYMMDD'` with `dayjs(date).format('YYYYMMDD')` before submitting.
- `alarm_id` is the autoincrement PK used for PUT/DELETE.
- After this ticket, the Settings domain is complete. Phase 7 gate: all CRUD operations work for accounts, codes, credit-cards, budgets, and alarms.
