# FE-028 — OtherAssetsView — tab shell + stocks list + stock CRUD

> Depends on: FE-012 (otherAssets store), FE-007 (api/otherAssets.ts), FE-015 (shared UI)

## Sub-tasks

| # | Task | Target file | Signature / key content | Acceptance command |
|---|------|-------------|-------------------------|--------------------|
| 1 | Read `GET /assets/stocks`, `POST /assets/stocks`, `PUT /assets/stocks/{stock_id}`, `DELETE /assets/stocks/{stock_id}` shapes in api-reference.md. | — | — | `grep -B2 -A 25 "GET /assets/stocks$" api/docs/api-reference.md \| head -30` |
| 2 | Replace `OtherAssetsView.vue` stub. Add `<el-tabs>` with 5 tabs: 股票, 房產, 保險, 貸款, 其他資產. Bind to `store.activeTab`. | `view/src/views/other-assets/OtherAssetsView.vue` | `const store = useOtherAssetsStore(); <el-tabs v-model="store.activeTab">` + 5 `<el-tab-pane name="stocks/estates/insurances/loans/other">` | `grep -q 'el-tabs' view/src/views/other-assets/OtherAssetsView.vue && grep -q 'activeTab' view/src/views/other-assets/OtherAssetsView.vue` |
| 3 | Call `store.fetchStocks()` on mount (or on 股票 tab activation). | `view/src/views/other-assets/OtherAssetsView.vue` | `onMounted(() => store.fetchStocks())` | `grep -q 'fetchStocks' view/src/views/other-assets/OtherAssetsView.vue` |
| 4 | Render stocks tab: `<el-table>` with columns for `stock_code`, `stock_name`, and key financial fields from the API response. Include edit/delete action column. Add "新增" button. | `view/src/views/other-assets/OtherAssetsView.vue` | `<el-table-column prop="stock_code">` etc. | `grep -q 'stock_code' view/src/views/other-assets/OtherAssetsView.vue` |
| 5 | Add stock create/edit dialog: form with all `StockAsset` required fields per api-reference.md. Submit: `createStock(data)` or `updateStock(id, data)` → `store.fetchStocks()`. | `view/src/views/other-assets/OtherAssetsView.vue` | `const stockDialogVisible = ref(false)` + form + submit handler | `grep -q 'stockDialogVisible\|createStock' view/src/views/other-assets/OtherAssetsView.vue` |
| 6 | Add delete with `<el-popconfirm>` per row: `deleteStock(id)` → `store.fetchStocks()`. | `view/src/views/other-assets/OtherAssetsView.vue` | `<el-popconfirm @confirm="handleDeleteStock(row.stock_id)">` | `grep -q 'deleteStock' view/src/views/other-assets/OtherAssetsView.vue` |
| 7 | Other 4 tabs (房產, 保險, 貸款, 其他資產) show `<EmptyState>` placeholder for now — they are implemented in FE-029..FE-033. | `view/src/views/other-assets/OtherAssetsView.vue` | `<EmptyState message="開發中" />` in each non-stocks tab pane | `grep -q 'EmptyState' view/src/views/other-assets/OtherAssetsView.vue` |
| 8 | Run type-check. | — | — | `cd view && npm run type-check 2>&1 \| tail -3` |

## Notes

- Read exact `StockAsset` field list from api-reference.md before building the form.
- `stock_id` is the autoincrement PK used for update/delete. The form does not include it.
