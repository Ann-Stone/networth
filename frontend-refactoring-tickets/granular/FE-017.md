# FE-017 — DashboardView — net-worth summary row

> Depends on: FE-009 (dashboard store), FE-015 (MoneyDisplay)

## Sub-tasks

| # | Task | Target file | Signature / key content | Acceptance command |
|---|------|-------------|-------------------------|--------------------|
| 1 | Read `GET /dashboard/summary` response shape in `api/docs/api-reference.md` before writing any code. | — | — | `grep -A 30 'GET /dashboard/summary' api/docs/api-reference.md \| head -30` |
| 2 | Replace the `DashboardView.vue` stub with a proper `<script setup lang="ts">` component that imports `useDashboardStore` and calls `fetchSummary()` on mount. | `view/src/views/DashboardView.vue` | `const store = useDashboardStore(); onMounted(() => store.fetchSummary())` | `grep -q 'fetchSummary' view/src/views/DashboardView.vue` |
| 3 | Render a 3-column summary metric grid styled after `template.html`: Total Assets, Total Liabilities, Net Worth. Each card shows the amount via `<MoneyDisplay>` and a percentage change badge if the API provides it. Show `<el-skeleton>` while loading. | `view/src/views/DashboardView.vue` | Grid: `class="grid grid-cols-1 md:grid-cols-3 gap-6"`. Each card: rounded-xl, surface-dark bg in dark mode, card shadow. | `grep -q 'MoneyDisplay' view/src/views/DashboardView.vue` |
| 4 | Add page title section: heading "儀表板" + subtitle with today's date. | `view/src/views/DashboardView.vue` | `<h1 class="text-3xl font-extrabold">儀表板</h1>` + `<p class="text-muted-text">{{ today }}</p>` using `dayjs().format('YYYY-MM-DD')` | `grep -q '儀表板' view/src/views/DashboardView.vue` |
| 5 | Run type-check. | — | — | `cd view && npm run type-check 2>&1 \| tail -3` |

## Notes

- Read the actual `DashboardSummary` shape from api-reference.md to know which fields to render. Map field names exactly (snake_case).
- If `GET /dashboard/summary` includes net worth trend data (e.g. month-over-month change %), render it as a small badge per `template.html` metric card style.
- Keep the component focused on the summary row only; other sections (alarms, targets, budget) are FE-018, FE-019, FE-020.
