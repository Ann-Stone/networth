# FE-022 — CashFlowView — journal CRUD dialogs

> Depends on: FE-021 (journal list), FE-004 (utilities.ts for selections), FE-005 (api/cashFlow.ts)

## Sub-tasks

| # | Task | Target file | Signature / key content | Acceptance command |
|---|------|-------------|-------------------------|--------------------|
| 1 | Read `POST /monthly-report/journals` request body shape in api-reference.md. | — | — | `grep -B2 -A 30 "POST /monthly-report/journals" api/docs/api-reference.md \| head -35` |
| 2 | Add "新增" button above the journal table. On click, open an `<el-dialog>` with a journal create/edit form. | `view/src/views/monthly-report/CashFlowView.vue` | `const journalDialogVisible = ref(false)` + `<el-dialog v-model="journalDialogVisible">` | `grep -q 'journalDialogVisible' view/src/views/monthly-report/CashFlowView.vue` |
| 3 | Journal form fields: `action_date` (date picker), `account_id` (select, options from `getAccountSelections()`), `action_main_type` (select: Floating/Fixed/Income/Transfer), `action_sub_type` (select, options from `getCodeSelections()` filtered by main type), `amount` (number input), `memo` (text input), `invoice_number` (text input, optional). | `view/src/views/monthly-report/CashFlowView.vue` | Form uses `el-form` + `el-form-item` with `el-select`, `el-input`, `el-date-picker`, `el-input-number`. Load selections on dialog open. | `grep -q 'getAccountSelections\|action_sub_type' view/src/views/monthly-report/CashFlowView.vue` |
| 4 | On form submit: call `createJournal(data)` or `updateJournal(id, data)`, then `store.fetchJournals()`. Close dialog on success. | `view/src/views/monthly-report/CashFlowView.vue` | `import { createJournal, updateJournal, deleteJournal } from '@/api/cashFlow'` + submit handler | `grep -q 'createJournal' view/src/views/monthly-report/CashFlowView.vue` |
| 5 | Add edit button per table row: click populates form with existing journal data and opens dialog in edit mode. | `view/src/views/monthly-report/CashFlowView.vue` | `const editJournal = (row: Journal) => { formData.value = { ...row }; journalDialogVisible.value = true }` | `grep -q 'editJournal\|updateJournal' view/src/views/monthly-report/CashFlowView.vue` |
| 6 | Add delete button per row with `<el-popconfirm>`. On confirm: call `deleteJournal(id)`, then `store.fetchJournals()`. | `view/src/views/monthly-report/CashFlowView.vue` | `<el-popconfirm @confirm="handleDeleteJournal(row.journal_id)">` | `grep -q 'deleteJournal' view/src/views/monthly-report/CashFlowView.vue` |
| 7 | Run type-check. | — | — | `cd view && npm run type-check 2>&1 \| tail -3` |

## Notes

- `account_id` is the business-key string, not a numeric ID. The selections dropdown from `getAccountSelections()` returns `{ account_id, name }` items.
- `action_sub_type` dropdown depends on `action_main_type`. Reload code options when main type changes.
- Form validation: `action_date`, `account_id`, `action_main_type`, `amount` are required.

**POST_FRONTEND_TODO.md §3 rows touched:**
`Journal.invoice_number` — rendered as an optional input field in the journal CRUD form.
- **Decision**: `keep` — the field is shown in the form for manual invoice entry.
- Set `Verified at` to `view/src/views/monthly-report/CashFlowView.vue` in the commit.
