# FE-034 — MenuSettingView — accounts CRUD tab

> Depends on: FE-008 (setting store), FE-003 (api/setting.ts)

## Sub-tasks

| # | Task | Target file | Signature / key content | Acceptance command |
|---|------|-------------|-------------------------|--------------------|
| 1 | Read `GET /settings/accounts`, `POST`, `PUT /settings/accounts/{id}`, `DELETE /settings/accounts/{id}` shapes in api-reference.md. | — | — | `grep -B2 -A 25 "GET /settings/accounts$" api/docs/api-reference.md \| head -30` |
| 2 | Replace `MenuSettingView.vue` stub. Add `<el-tabs>` with 3 tabs: 帳戶, 分類, 信用卡. Import and use `useSettingStore`. Call `store.fetchAccounts()` on mount. | `view/src/views/setting/MenuSettingView.vue` | `const store = useSettingStore(); onMounted(() => store.fetchAccounts())` + `<el-tabs v-model="activeTab">` | `grep -q 'useSettingStore\|el-tabs' view/src/views/setting/MenuSettingView.vue` |
| 3 | Render accounts tab: `<el-table :data="store.accounts">`. Columns: `name`, `account_type`, `fx_code`, `in_use` (via `<StatusBadge>`), `discount`, `owner`, `account_index`. Include add/edit/delete actions. | `view/src/views/setting/MenuSettingView.vue` | Table with `v-loading="store.accountsLoading"` | `grep -q 'account_index\|discount\|owner' view/src/views/setting/MenuSettingView.vue` |
| 4 | Account create/edit dialog: form with all fields from `AccountCreate`. Required: `account_id`, `name`, `account_type`, `fx_code`. Optional: `is_calculate`, `in_use`, `discount`, `memo`, `owner`, `account_index`. | `view/src/views/setting/MenuSettingView.vue` | `const accountDialogVisible = ref(false)` + form + submit: `createAccount/updateAccount` → `store.fetchAccounts()` | `grep -q 'accountDialogVisible\|createAccount' view/src/views/setting/MenuSettingView.vue` |
| 5 | Delete with `<el-popconfirm>`: `deleteAccount(id)` → `store.fetchAccounts()`. | `view/src/views/setting/MenuSettingView.vue` | `deleteAccount` in confirm handler | `grep -q 'deleteAccount' view/src/views/setting/MenuSettingView.vue` |
| 6 | 分類 and 信用卡 tabs show `<EmptyState message="開發中" />` for now — implemented in FE-035 and FE-036. | `view/src/views/setting/MenuSettingView.vue` | `<EmptyState>` in 2 tab panes | `grep -q 'EmptyState' view/src/views/setting/MenuSettingView.vue` |
| 7 | Run type-check. | — | — | `cd view && npm run type-check 2>&1 \| tail -3` |

## Notes

- `account_id` is a user-supplied business key (e.g. `"BANK-CHASE-01"`), not the autoincrement `id`. The autoincrement `id` is used for PUT/DELETE URL parameter.

**POST_FRONTEND_TODO.md §3 rows touched:**
`Account.discount`, `Account.owner`, `Account.account_index` — all rendered as table columns and form fields.
- **Decision for all three**: `keep` — all three fields are shown in the accounts management table.
- Set `Verified at` to `view/src/views/setting/MenuSettingView.vue` in the same commit.
