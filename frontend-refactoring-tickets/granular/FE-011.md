# FE-011 — Create src/stores/yearReport.ts

> Depends on: FE-006 (api/yearReport.ts), FE-002 (types/models.ts)

## Sub-tasks

| # | Task | Target file | Signature / key content | Acceptance command |
|---|------|-------------|-------------------------|--------------------|
| 1 | Create `src/stores/yearReport.ts`. Export `useYearReportStore`. | `view/src/stores/yearReport.ts` | `export const useYearReportStore = defineStore('yearReport', () => { ... })` | `test -f view/src/stores/yearReport.ts && grep -q 'useYearReportStore' view/src/stores/yearReport.ts` |
| 2 | Add selected year state: `selectedYear: ref<number>(new Date().getFullYear())`. | `view/src/stores/yearReport.ts` | `const selectedYear = ref(new Date().getFullYear())` | `grep -q 'selectedYear' view/src/stores/yearReport.ts` |
| 3 | Add balance report state + fetch: `balanceReport: ref<BalanceReport \| null>(null)`, `balanceLoading`, `fetchBalanceReport(year?)`. | `view/src/stores/yearReport.ts` | `async function fetchBalanceReport(year?: number)` | `grep -q 'fetchBalanceReport' view/src/stores/yearReport.ts` |
| 4 | Add expenditure report state + fetch: `expenditureReport: ref<ExpenditureReport \| null>(null)`, `expenditureLoading`, `fetchExpenditureReport(type, year?)`. | `view/src/stores/yearReport.ts` | `async function fetchExpenditureReport(type: string, year?: number)` | `grep -q 'fetchExpenditureReport' view/src/stores/yearReport.ts` |
| 5 | Add assets report state + fetch: `assetsReport: ref<AssetReport \| null>(null)`, `assetsLoading`, `fetchAssetsReport(year?)`. | `view/src/stores/yearReport.ts` | Standard store pattern | `grep -q 'fetchAssetsReport' view/src/stores/yearReport.ts` |
| 6 | Return all state + functions. | `view/src/stores/yearReport.ts` | `return { selectedYear, balanceReport, balanceLoading, fetchBalanceReport, ... }` | `grep -q 'return {' view/src/stores/yearReport.ts` |
| 7 | Run type-check. | — | — | `cd view && npm run type-check 2>&1 \| tail -3` |

## Notes

- `selectedYear` is shared across the three year-report views. Changing year in one view should re-trigger fetches in others if they're active.
- Do not import any other store.
