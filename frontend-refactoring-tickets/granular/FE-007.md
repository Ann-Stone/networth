# FE-007 — Create src/api/otherAssets.ts

> Depends on: FE-001 (request.ts), FE-002 (types/models.ts)

## Sub-tasks

| # | Task | Target file | Signature / key content | Acceptance command |
|---|------|-------------|-------------------------|--------------------|
| 1 | Create `src/api/otherAssets.ts`. Implement Stock functions: `getStocks()`, `createStock(data)`, `updateStock(id, data)`, `deleteStock(id)`. Map to CRUD on `/assets/stocks` and `/assets/stocks/{stock_id}`. | `view/src/api/otherAssets.ts` | `export function getStocks(): Promise<StockAsset[]>` | `test -f view/src/api/otherAssets.ts && grep -q 'getStocks' view/src/api/otherAssets.ts` |
| 2 | Implement Stock journal/detail functions: `getStockDetails(stockId)`, `createStockDetail(stockId, data)`, `updateStockDetail(distinctNumber, data)`, `deleteStockDetail(distinctNumber)`. Map to `/assets/stocks/{stock_id}/details` and `/assets/stocks/details/{distinct_number}`. | `view/src/api/otherAssets.ts` | `export function getStockDetails(stockId: number): Promise<StockJournal[]>` | `grep -q 'getStockDetails' view/src/api/otherAssets.ts && grep -q 'deleteStockDetail' view/src/api/otherAssets.ts` |
| 3 | Implement Estate functions: `getEstates()`, `createEstate(data)`, `updateEstate(id, data)`, `deleteEstate(id)`, `getEstateDetails(estateId)`, `createEstateDetail(estateId, data)`, `updateEstateDetail(distinctNumber, data)`, `deleteEstateDetail(distinctNumber)`. | `view/src/api/otherAssets.ts` | `export function getEstates(): Promise<EstateAsset[]>` | `grep -q 'getEstates' view/src/api/otherAssets.ts && grep -q 'getEstateDetails' view/src/api/otherAssets.ts` |
| 4 | Implement Insurance functions: full CRUD on `/assets/insurances` + `/assets/insurances/{id}/details` + `/assets/insurances/details/{distinct_number}`. Same pattern as Estate. | `view/src/api/otherAssets.ts` | `export function getInsurances(): Promise<InsuranceAsset[]>` | `grep -q 'getInsurances' view/src/api/otherAssets.ts && grep -q 'deleteInsuranceDetail' view/src/api/otherAssets.ts` |
| 5 | Implement Loan functions: full CRUD on `/assets/loans` + `/assets/loans/{id}/details` + `/assets/loans/details/{distinct_number}`. Include `getLoanSelection()` → `GET /assets/loans/selection`. | `view/src/api/otherAssets.ts` | `export function getLoans(): Promise<LoanAsset[]>` | `grep -q 'getLoans' view/src/api/otherAssets.ts && grep -q 'getLoanSelection' view/src/api/otherAssets.ts` |
| 6 | Implement OtherAsset functions: `getOtherAssets()`, `getOtherAssetItems()`, `createOtherAsset(data)`, `updateOtherAsset(id, data)`, `deleteOtherAsset(id)`. Map to `/assets/other-assets` and `/assets/other-assets/items`. | `view/src/api/otherAssets.ts` | `export function getOtherAssets(): Promise<OtherAsset[]>` | `grep -q 'getOtherAssets' view/src/api/otherAssets.ts && grep -q 'getOtherAssetItems' view/src/api/otherAssets.ts` |
| 7 | Run type-check — must pass with zero errors. | — | — | `cd view && npm run type-check 2>&1 \| tail -3` |

## Notes

- Read `api/docs/api-reference.md` Assets section for all response shapes.
- `distinct_number` is the PK for detail/journal rows across all asset types.
- `GET /assets/loans/selection` returns a lightweight list for loan dropdowns (used in journal CRUD).
- `GET /assets/other-assets/items` returns a flat list of all OtherAsset entries (vs. `getOtherAssets()` which may return grouped data — check api-reference.md for the difference).

**POST_FRONTEND_TODO.md §3 rows touched:**
`Stock_Journal.expected_spend` — field is in `StockJournal` type. Keep/drop decision deferred to FE-029.
`Loan.grace_expire_date` — field is in `LoanAsset` type. Keep/drop decision deferred to FE-032.
