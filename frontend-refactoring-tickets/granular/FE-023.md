# FE-023 — CashFlowView — analytics chart tabs

> Depends on: FE-021 (CashFlowView scaffold), FE-010 (cashFlow store), FE-016 (chart components)

## Sub-tasks

| # | Task | Target file | Signature / key content | Acceptance command |
|---|------|-------------|-------------------------|--------------------|
| 1 | Read the four analytics endpoint response shapes: `expenditure-budget`, `expenditure-ratio`, `invest-ratio`, `liability` (all under `/monthly-report/journals/{month}/`). | — | — | `grep -B2 -A 20 "expenditure-budget" api/docs/api-reference.md \| head -25` |
| 2 | Add a tab section below the journal table using `<el-tabs>`. Tabs: 收支預算, 支出比例, 投資比例, 負債. | `view/src/views/monthly-report/CashFlowView.vue` | `<el-tabs v-model="activeChartTab">` with 4 `<el-tab-pane>` | `grep -q 'el-tabs' view/src/views/monthly-report/CashFlowView.vue` |
| 3 | On tab change (or on month change), fetch the relevant analytics data from the store. Lazy-fetch: only load a tab's data when the tab is first activated. | `view/src/views/monthly-report/CashFlowView.vue` | `watch(activeChartTab, (tab) => { if (tab === 'budget') store.fetchExpenditureBudget() ... })` | `grep -q 'fetchExpenditureBudget' view/src/views/monthly-report/CashFlowView.vue` |
| 4 | 收支預算 tab: render `<BarChart>` with budget vs actual by category. | `view/src/views/monthly-report/CashFlowView.vue` | Transform `store.expenditureBudget` into `xData` + `series` for BarChart | `grep -q 'expenditureBudget' view/src/views/monthly-report/CashFlowView.vue` |
| 5 | 支出比例 tab: render `<DonutChart>` with expense breakdown by category. | `view/src/views/monthly-report/CashFlowView.vue` | Transform `store.expenditureRatio` into pie `data` array | `grep -q 'expenditureRatio' view/src/views/monthly-report/CashFlowView.vue` |
| 6 | 投資比例 tab: render `<DonutChart>` for investment allocation. | `view/src/views/monthly-report/CashFlowView.vue` | Same pattern | `grep -q 'investRatio' view/src/views/monthly-report/CashFlowView.vue` |
| 7 | 負債 tab: render a table or summary card for liability data. | `view/src/views/monthly-report/CashFlowView.vue` | Table or card depending on `JournalLiability` shape | `grep -q 'liability\|Liability' view/src/views/monthly-report/CashFlowView.vue` |
| 8 | Run type-check. | — | — | `cd view && npm run type-check 2>&1 \| tail -3` |

## Notes

- All analytics data in the store resets when `selectedMonth` changes; views must re-fetch on month change.
- Handle empty/null data gracefully with `<EmptyState>` inside each tab pane.
