# FE-041 — Build verification — npm run build + npm run build:mock

> Depends on: FE-040 (MSW wired), all previous tickets

## Sub-tasks

| # | Task | Target file | Signature / key content | Acceptance command |
|---|------|-------------|-------------------------|--------------------|
| 1 | Run `npm run type-check` — must exit zero with no errors. Fix any remaining type errors before proceeding. | — | — | `cd view && npm run type-check 2>&1 \| tail -5` |
| 2 | Run `npm run build` — production build (no mock). Must succeed. | — | — | `cd view && npm run build 2>&1 \| tail -10` |
| 3 | Run `npm run build:mock` — GitHub Pages build with mock mode enabled. Must succeed. | — | — | `cd view && npm run build:mock 2>&1 \| tail -10` |
| 4 | Verify `dist/` and `dist-mock/` (or whatever `build:mock` outputs) both contain `index.html` and bundle assets. | — | — | `test -f view/dist/index.html` |
| 5 | Start dev server (`npm run dev`) and navigate to `/networth/dashboard` — verify the page loads and the summary cards are populated (either via API proxy or mock). | — | — | `cd view && npm run dev &; sleep 5; curl -s http://127.0.0.1:5173/networth/ \| grep -q 'WarmBalance\|Balance Sheet'; pkill -f 'vite'` |
| 6 | Review `POST_FRONTEND_TODO.md §3` — verify every row has a non-`_pending_` value in `Verified at`, `Decision`, and `Frontend commit`. If any row is still pending, flag it in the session summary without declaring the phase done. | — | — | `grep -c '_pending_' POST_FRONTEND_TODO.md` (should return 0) |

## Notes

- `npm run build` uses `vue-tsc -b && vite build`. The `vue-tsc -b` step often catches errors that `npm run type-check` misses in `.vue` template expressions. Fix any template-level type errors before finalizing.
- If `build:mock` fails because MSW service worker path differs, adjust `vite.config.ts` to serve `public/mockServiceWorker.js` correctly.
- After this ticket the refactor is complete. Prepare the session summary with:
  1. List of all tickets completed this phase (FE-039..FE-041)
  2. Type-check status: pass
  3. Build status: pass (both `build` and `build:mock`)
  4. `§3 updates this phase` — any rows resolved in Phase 8
  5. Any flagged gaps or follow-up items
