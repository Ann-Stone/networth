# FE-030 — OtherAssetsView — estates tab (parent + details CRUD)

> Depends on: FE-028 (tab shell exists), FE-007 (api/otherAssets.ts)

## Sub-tasks

| # | Task | Target file | Signature / key content | Acceptance command |
|---|------|-------------|-------------------------|--------------------|
| 1 | Read estate API shapes: `GET /assets/estates`, `POST`, `PUT /assets/estates/{estate_id}`, `DELETE /assets/estates/{estate_id}`, `GET /assets/estates/{estate_id}/details`, `POST /assets/estates/{estate_id}/details`, `PUT/DELETE /assets/estates/details/{distinct_number}`. | — | — | `grep -B2 -A 20 "GET /assets/estates$" api/docs/api-reference.md \| head -25` |
| 2 | Implement 房產 tab content. Call `store.fetchEstates()` when tab is activated (use `onActivated` or tab-change watcher). | `view/src/views/other-assets/OtherAssetsView.vue` | Replace `<EmptyState>` in estates tab pane with `<el-table :data="store.estates">` | `grep -q 'fetchEstates' view/src/views/other-assets/OtherAssetsView.vue` |
| 3 | Estate table columns: key fields from `EstateAsset` per api-reference.md. Include expand row for details + add/edit/delete actions. | `view/src/views/other-assets/OtherAssetsView.vue` | `<el-table-column>` per field + actions column | `grep -q 'estate_id\|estates' view/src/views/other-assets/OtherAssetsView.vue` |
| 4 | Estate create/edit dialog: form with all required fields. Submit: `createEstate/updateEstate` → `store.fetchEstates()`. | `view/src/views/other-assets/OtherAssetsView.vue` | `const estateDialogVisible = ref(false)` | `grep -q 'estateDialogVisible\|createEstate' view/src/views/other-assets/OtherAssetsView.vue` |
| 5 | Estate details expand: on expand call `store.fetchEstateDetails(estateId)`. Render nested table with `store.estateDetails`. CRUD for detail rows: `createEstateDetail / updateEstateDetail / deleteEstateDetail`. | `view/src/views/other-assets/OtherAssetsView.vue` | Nested table inside expand + dialog for detail CRUD | `grep -q 'fetchEstateDetails\|createEstateDetail' view/src/views/other-assets/OtherAssetsView.vue` |
| 6 | Run type-check. | — | — | `cd view && npm run type-check 2>&1 \| tail -3` |
