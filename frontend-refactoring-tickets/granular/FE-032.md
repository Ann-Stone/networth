# FE-032 — OtherAssetsView — loans tab (parent + details CRUD)

> Depends on: FE-028 (tab shell), FE-007 (api/otherAssets.ts)

## Sub-tasks

| # | Task | Target file | Signature / key content | Acceptance command |
|---|------|-------------|-------------------------|--------------------|
| 1 | Read loan API shapes: `GET /assets/loans`, `POST`, `GET /assets/loans/{loan_id}`, `PUT/DELETE /assets/loans/{loan_id}`, loan details endpoints, `GET /assets/loans/selection`. | — | — | `grep -B2 -A 25 "GET /assets/loans$" api/docs/api-reference.md \| head -30` |
| 2 | Implement 貸款 tab: call `store.fetchLoans()` on tab activation. Replace `<EmptyState>` with loans table. | `view/src/views/other-assets/OtherAssetsView.vue` | `<el-table :data="store.loans">` | `grep -q 'fetchLoans' view/src/views/other-assets/OtherAssetsView.vue` |
| 3 | Loan table columns: key fields from `LoanAsset` per api-reference.md. Include `grace_expire_date` column if the field exists in the API response. | `view/src/views/other-assets/OtherAssetsView.vue` | Check api-reference.md for `grace_expire_date` field presence | `grep -q 'loan_id' view/src/views/other-assets/OtherAssetsView.vue` |
| 4 | Loan create/edit dialog: form with required fields. Include `grace_expire_date` field if present in API. Submit: `createLoan / updateLoan` → `store.fetchLoans()`. | `view/src/views/other-assets/OtherAssetsView.vue` | `const loanDialogVisible = ref(false)` | `grep -q 'loanDialogVisible\|createLoan' view/src/views/other-assets/OtherAssetsView.vue` |
| 5 | Loan details expand: `store.fetchLoanDetails(loanId)` on expand. CRUD: `createLoanDetail / updateLoanDetail / deleteLoanDetail`. | `view/src/views/other-assets/OtherAssetsView.vue` | Same pattern as previous asset types | `grep -q 'fetchLoanDetails' view/src/views/other-assets/OtherAssetsView.vue` |
| 6 | Run type-check. | — | — | `cd view && npm run type-check 2>&1 \| tail -3` |

## Notes

- `GET /assets/loans/selection` is used for dropdowns elsewhere (journal CRUD). It is already exposed via `api/otherAssets.ts` `getLoanSelection()`.

**POST_FRONTEND_TODO.md §3 rows touched:**
`Loan.grace_expire_date` — verify whether this field appears in the `GET /assets/loans` response per api-reference.md.
- If the API returns `grace_expire_date` and you render it in the table/form: **Decision = `keep`**.
- If the field is not in the API response (was dropped in BE): **Decision = `drop`**, note that it's absent from the spec.
- Set `Verified at` to `view/src/views/other-assets/OtherAssetsView.vue` in the same commit.
