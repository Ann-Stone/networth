# FE-039 — Utilities import panel (stock-prices, fx-rates, invoices CSV)

> Depends on: FE-014 (ImportView.vue stub + route), FE-004 (api/utilities.ts)

## Sub-tasks

| # | Task | Target file | Signature / key content | Acceptance command |
|---|------|-------------|-------------------------|--------------------|
| 1 | Read `POST /utilities/import/stock-prices`, `/utilities/import/fx-rates`, `/utilities/import/invoices` shapes in api-reference.md. Understand expected file formats. | — | — | `grep -B2 -A 20 "POST /utilities/import" api/docs/api-reference.md \| head -25` |
| 2 | Implement `ImportView.vue`. Add 3 import sections: 股價匯入, 匯率匯入, 發票匯入. Each section has a file picker and an "上傳" button. | `view/src/views/utilities/ImportView.vue` | Three `<el-card>` sections, each with `<el-upload :auto-upload="false">` and an upload button | `grep -q 'el-upload\|importStockPrices' view/src/views/utilities/ImportView.vue` |
| 3 | Stock price upload handler: calls `importStockPrices(file)` from `@/api/utilities.ts`. Show loading state and success/error toast. | `view/src/views/utilities/ImportView.vue` | `async function handleStockPriceUpload(file: File)` | `grep -q 'importStockPrices' view/src/views/utilities/ImportView.vue` |
| 4 | FX rate upload handler: calls `importFxRates(file)`. | `view/src/views/utilities/ImportView.vue` | `async function handleFxRateUpload(file: File)` | `grep -q 'importFxRates' view/src/views/utilities/ImportView.vue` |
| 5 | Invoice upload handler: calls `importInvoices(file)`. | `view/src/views/utilities/ImportView.vue` | `async function handleInvoiceUpload(file: File)` | `grep -q 'importInvoices' view/src/views/utilities/ImportView.vue` |
| 6 | Run type-check. | — | — | `cd view && npm run type-check 2>&1 \| tail -3` |

## Notes

- Import endpoints run as background tasks on the backend; the API responds immediately with success. The frontend shows a success toast but cannot track completion.
- Show the expected file format/extension as hint text next to each upload area (e.g. `.csv` for invoices).
- `el-upload` with `:auto-upload="false"` lets the user select a file, then the button triggers the actual upload. Use the `onChange` / `before-upload` hook to get the `File` object.
