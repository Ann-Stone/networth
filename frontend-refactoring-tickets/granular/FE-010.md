# FE-010 — Create src/stores/cashFlow.ts

> Depends on: FE-005 (api/cashFlow.ts), FE-002 (types/models.ts)

## Sub-tasks

| # | Task | Target file | Signature / key content | Acceptance command |
|---|------|-------------|-------------------------|--------------------|
| 1 | Create `src/stores/cashFlow.ts`. Export `useCashFlowStore`. | `view/src/stores/cashFlow.ts` | `export const useCashFlowStore = defineStore('cashFlow', () => { ... })` | `test -f view/src/stores/cashFlow.ts && grep -q 'useCashFlowStore' view/src/stores/cashFlow.ts` |
| 2 | Add selected month state: `selectedMonth: ref<string>('')` (format `YYYYMM`, initialized to current month on store creation). | `view/src/stores/cashFlow.ts` | `import dayjs from 'dayjs'` + `const selectedMonth = ref(dayjs().format('YYYYMM'))` | `grep -q 'selectedMonth' view/src/stores/cashFlow.ts` |
| 3 | Add journal state + fetch: `journals: ref<Journal[]>([])`, `journalsLoading`, `fetchJournals(month?)`. If `month` omitted, uses `selectedMonth.value`. | `view/src/stores/cashFlow.ts` | `async function fetchJournals(month?: string) { const m = month ?? selectedMonth.value; ... }` | `grep -q 'fetchJournals' view/src/stores/cashFlow.ts` |
| 4 | Add analytics state + fetch: `expenditureBudget`, `expenditureRatio`, `investRatio`, `liability` (all nullable with loading flags), and corresponding `fetchExpenditureBudget(month?)` etc. | `view/src/stores/cashFlow.ts` | `const expenditureBudget = ref<JournalExpenditureBudget \| null>(null)` | `grep -q 'fetchExpenditureBudget' view/src/stores/cashFlow.ts && grep -q 'fetchLiability' view/src/stores/cashFlow.ts` |
| 5 | Add stock prices state + fetch: `stockPrices: ref<StockPriceRead[]>([])`, `stockPricesLoading`, `fetchStockPrices(month?)`. | `view/src/stores/cashFlow.ts` | Standard store pattern | `grep -q 'fetchStockPrices' view/src/stores/cashFlow.ts` |
| 6 | Return all state + functions. | `view/src/stores/cashFlow.ts` | `return { selectedMonth, journals, journalsLoading, fetchJournals, ... }` | `grep -q 'return {' view/src/stores/cashFlow.ts` |
| 7 | Run type-check. | — | — | `cd view && npm run type-check 2>&1 \| tail -3` |

## Notes

- `selectedMonth` is the view's currently-displayed month. Views call `store.selectedMonth = 'YYYYMM'` when the user changes the month picker, then call `fetchJournals()`.
- `settleMonth` and `uploadStockPrices` are not stored state — views call the API functions directly and refresh store data afterward.
