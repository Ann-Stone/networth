# FE-014 — Update Sidebar.vue + SidebarContent.vue for full route coverage

> Depends on: FE-013 (palette in main.css), Phase 0 complete (router routes exist)

## Sub-tasks

| # | Task | Target file | Signature / key content | Acceptance command |
|---|------|-------------|-------------------------|--------------------|
| 1 | Read existing `Sidebar.vue` and `SidebarContent.vue` before editing. | — | — | `test -f view/src/components/layout/Sidebar.vue` |
| 2 | Ensure the sidebar renders all navigation groups and links matching the router. Groups: (a) 月報 → 現金流 (`/monthly-report/cash-flow`); (b) 年報 → 資產負債表, 年度支出, 資產概覽 (`/year-report/*`); (c) 資產管理 → `/other-assets`; (d) 設定 → 選單設定, 預算設定, 提醒設定 (`/setting/*`); (e) 工具 → 資料匯入 (`/utilities/import`). | `view/src/components/layout/SidebarContent.vue` | `router-link` or `@click` with `router.push` for each route listed above | `grep -q '/monthly-report/cash-flow' view/src/components/layout/SidebarContent.vue` |
| 3 | Style the active route highlight using the new sage-green primary: active item gets `bg-primary/15 text-primary` classes (matching `template.html` active nav style). Use `router-link-active` class or `useRoute` to detect active path. | `view/src/components/layout/SidebarContent.vue` | `<router-link :class="[$route.path.startsWith(item.path) ? 'bg-primary/15 text-primary' : 'text-muted-text hover:bg-surface-dark']" ...>` | `grep -q 'router-link' view/src/components/layout/SidebarContent.vue` |
| 4 | Add collapse/expand toggle to sidebar (already in `useAppStore.sidebarCollapsed`). When collapsed, show only icons; when expanded show icon + label. | `view/src/components/layout/Sidebar.vue` | `v-if="!appStore.sidebarCollapsed"` on the text label spans | `grep -q 'sidebarCollapsed' view/src/components/layout/Sidebar.vue` |
| 5 | Add the `/utilities/import` route to `src/router/index.ts` if not present. | `view/src/router/index.ts` | `{ path: 'utilities/import', name: 'UtilitiesImport', component: () => import('@/views/utilities/ImportView.vue'), meta: { title: '資料匯入' } }` | `grep -q 'utilities' view/src/router/index.ts` |
| 6 | Create stub `src/views/utilities/ImportView.vue` (Phase 8 will implement it). | `view/src/views/utilities/ImportView.vue` | `<template><div><h1>資料匯入</h1></div></template>` | `test -f view/src/views/utilities/ImportView.vue` |
| 7 | Run `npm run dev` and visually verify sidebar renders all routes and active state is correct. | — | — | `cd view && npm run build 2>&1 \| tail -5` |

## Notes

- Do not use `vue-router`'s `<router-link-active-class>` global config — use per-link conditional classes for precise control over which prefix triggers the active style.
- The sidebar groups in `template.html` are labeled "Main Navigation" and "Internal Audit" — adapt to the actual app sections (月報, 年報, 資產管理, 設定, 工具).
- Mobile drawer behavior is already handled by `useAppStore`. Keep existing `sidebarDrawerVisible` logic.
- `Dashboard` link (`/dashboard`) should be at the top of the nav, not inside any group.
