# FE-036 — MenuSettingView — credit-cards CRUD tab

> Depends on: FE-034 (tab shell exists), FE-008 (setting store)

## Sub-tasks

| # | Task | Target file | Signature / key content | Acceptance command |
|---|------|-------------|-------------------------|--------------------|
| 1 | Read `GET /settings/credit-cards`, `POST`, `PUT/DELETE /settings/credit-cards/{credit_card_id}` shapes in api-reference.md. | — | — | `grep -B2 -A 25 "GET /settings/credit-cards$" api/docs/api-reference.md \| head -30` |
| 2 | Call `store.fetchCreditCards()` on 信用卡 tab activation. | `view/src/views/setting/MenuSettingView.vue` | Add `'credit-cards'` case to tab-change watcher | `grep -q 'fetchCreditCards' view/src/views/setting/MenuSettingView.vue` |
| 3 | Render credit-cards tab: `<el-table :data="store.creditCards">`. Columns: `name`, `card_no`, `billing_day`, `payment_day`, `in_use`, `credit_card_index`. | `view/src/views/setting/MenuSettingView.vue` | `<el-table :data="store.creditCards" v-loading="store.creditCardsLoading">` | `grep -q 'creditCards\|card_no' view/src/views/setting/MenuSettingView.vue` |
| 4 | Credit-card create/edit dialog: form with all `CreditCardCreate` fields. Submit: `createCreditCard/updateCreditCard` → `store.fetchCreditCards()`. | `view/src/views/setting/MenuSettingView.vue` | `const creditCardDialogVisible = ref(false)` | `grep -q 'creditCardDialogVisible\|createCreditCard' view/src/views/setting/MenuSettingView.vue` |
| 5 | Delete with `<el-popconfirm>`: `deleteCreditCard(id)` → `store.fetchCreditCards()`. | `view/src/views/setting/MenuSettingView.vue` | `deleteCreditCard` call | `grep -q 'deleteCreditCard' view/src/views/setting/MenuSettingView.vue` |
| 6 | Run type-check. | — | — | `cd view && npm run type-check 2>&1 \| tail -3` |

## Notes

- `credit_card_id` in the API is the business key (string). The PUT/DELETE URL uses `credit_card_id`, not an autoincrement id — read api-reference.md to confirm.

**POST_FRONTEND_TODO.md §3 rows touched:**
`Credit_Card.credit_card_index` — rendered as a table column.
- **Decision**: `keep` — shown in the credit card management table.
- Set `Verified at` to `view/src/views/setting/MenuSettingView.vue` in the same commit.
