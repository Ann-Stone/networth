# FE-029 — OtherAssetsView — stock journal detail sub-tab

> Depends on: FE-028 (stocks list), FE-007 (api/otherAssets.ts)

## Sub-tasks

| # | Task | Target file | Signature / key content | Acceptance command |
|---|------|-------------|-------------------------|--------------------|
| 1 | Read `GET /assets/stocks/{stock_id}/details`, `POST /assets/stocks/{stock_id}/details`, `PUT /assets/stocks/details/{distinct_number}`, `DELETE /assets/stocks/details/{distinct_number}` shapes in api-reference.md. | — | — | `grep -B2 -A 25 "GET /assets/stocks/{stock_id}/details" api/docs/api-reference.md \| head -30` |
| 2 | Add a detail/journal expand row (or nested panel) below each stock in the stocks table. When the user expands/clicks a stock row, call `store.fetchStockDetails(row.stock_id)`. | `view/src/views/other-assets/OtherAssetsView.vue` | `<el-table-column type="expand">` template + `store.fetchStockDetails(row.stock_id)` in expand handler | `grep -q 'fetchStockDetails' view/src/views/other-assets/OtherAssetsView.vue` |
| 3 | In the expanded row, render `store.stockDetails` in a nested table with journal fields: `action_date`, `buy_price` or `sell_price`, `quantity`, `expected_spend`, `memo`. Include add/edit/delete for journal rows. | `view/src/views/other-assets/OtherAssetsView.vue` | Nested `<el-table :data="store.stockDetails">` inside the expand template | `grep -q 'stockDetails' view/src/views/other-assets/OtherAssetsView.vue` |
| 4 | Add stock journal create/edit dialog: form with `StockJournal` fields. Submit: `createStockDetail(stockId, data)` or `updateStockDetail(distinctNumber, data)` → `store.fetchStockDetails(stockId)`. | `view/src/views/other-assets/OtherAssetsView.vue` | `const stockDetailDialogVisible = ref(false)` + form | `grep -q 'createStockDetail\|stockDetailDialogVisible' view/src/views/other-assets/OtherAssetsView.vue` |
| 5 | Add delete for journal rows: `<el-popconfirm>` → `deleteStockDetail(distinctNumber)` → `store.fetchStockDetails(stockId)`. | `view/src/views/other-assets/OtherAssetsView.vue` | `deleteStockDetail` call in confirm handler | `grep -q 'deleteStockDetail' view/src/views/other-assets/OtherAssetsView.vue` |
| 6 | Run type-check. | — | — | `cd view && npm run type-check 2>&1 \| tail -3` |

## Notes

- `distinct_number` is the PK for stock journal rows. Use it for PUT/DELETE, not `stock_id`.
- Read exact `StockJournal` field list from api-reference.md.

**POST_FRONTEND_TODO.md §3 rows touched:**
`Stock_Journal.expected_spend` — rendered as a column in the stock journal detail table and as a form field.
- **Decision**: to be determined during implementation. If the field is displayed in the table or form, set to `keep`. If you implement this view and decide not to show it, set to `drop`.
- Set `Verified at` to `view/src/views/other-assets/OtherAssetsView.vue` in the same commit.
