# FE-025 — BalanceSheetView (GET /reports/balance)

> Depends on: FE-011 (yearReport store), FE-016 (charts)

## Sub-tasks

| # | Task | Target file | Signature / key content | Acceptance command |
|---|------|-------------|-------------------------|--------------------|
| 1 | Read `GET /reports/balance` response shape in api-reference.md. Note all fields and any nested arrays. | — | — | `grep -B2 -A 40 "GET /reports/balance" api/docs/api-reference.md \| head -45` |
| 2 | Replace `BalanceSheetView.vue` stub. Import `useYearReportStore`. Call `store.fetchBalanceReport()` on mount. | `view/src/views/year-report/BalanceSheetView.vue` | `const store = useYearReportStore(); onMounted(() => store.fetchBalanceReport(store.selectedYear))` | `grep -q 'fetchBalanceReport' view/src/views/year-report/BalanceSheetView.vue` |
| 3 | Add year selector: `<el-date-picker type="year">` bound to `store.selectedYear`. On change, re-fetch. | `view/src/views/year-report/BalanceSheetView.vue` | Year picker with `v-model` + watcher that calls `store.fetchBalanceReport(year)` | `grep -q 'selectedYear' view/src/views/year-report/BalanceSheetView.vue` |
| 4 | Render the balance sheet data. If the response provides monthly time-series, render a `<LineChart>` showing net worth trend over the year. Render asset vs liability breakdown as a `<BarChart>` or `<el-table>`. | `view/src/views/year-report/BalanceSheetView.vue` | Choose chart type based on actual data shape from api-reference.md | `grep -q 'LineChart\|BarChart\|el-table' view/src/views/year-report/BalanceSheetView.vue` |
| 5 | Show `<el-skeleton>` while `store.balanceLoading`. Show `<EmptyState>` if data is null after load. | `view/src/views/year-report/BalanceSheetView.vue` | `v-if="store.balanceLoading"` + `v-else-if="!store.balanceReport"` | `grep -q 'balanceLoading' view/src/views/year-report/BalanceSheetView.vue` |
| 6 | Run type-check. | — | — | `cd view && npm run type-check 2>&1 \| tail -3` |

## Notes

- Read the actual `BalanceReport` shape from api-reference.md before designing the layout. Do not assume the shape.
- The year selector should default to the current year (already set in `yearReport.ts` store).
