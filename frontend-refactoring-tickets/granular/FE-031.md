# FE-031 — OtherAssetsView — insurances tab (parent + details CRUD)

> Depends on: FE-028 (tab shell), FE-007 (api/otherAssets.ts)

## Sub-tasks

| # | Task | Target file | Signature / key content | Acceptance command |
|---|------|-------------|-------------------------|--------------------|
| 1 | Read insurance API shapes: `GET /assets/insurances`, `POST`, `PUT/DELETE /assets/insurances/{insurance_id}`, `GET /assets/insurances/{insurance_id}/details`, detail CRUD endpoints. | — | — | `grep -B2 -A 20 "GET /assets/insurances$" api/docs/api-reference.md \| head -25` |
| 2 | Implement 保險 tab: call `store.fetchInsurances()` on tab activation. Replace `<EmptyState>` with insurance table. | `view/src/views/other-assets/OtherAssetsView.vue` | `<el-table :data="store.insurances">` | `grep -q 'fetchInsurances' view/src/views/other-assets/OtherAssetsView.vue` |
| 3 | Insurance table columns: key fields from `InsuranceAsset` per api-reference.md. Add/edit/delete actions. | `view/src/views/other-assets/OtherAssetsView.vue` | `insurance_id`, `policy_name` or equivalent fields per spec | `grep -q 'insurance_id' view/src/views/other-assets/OtherAssetsView.vue` |
| 4 | Insurance create/edit dialog: form with required fields. Submit: `createInsurance / updateInsurance` → `store.fetchInsurances()`. | `view/src/views/other-assets/OtherAssetsView.vue` | `const insuranceDialogVisible = ref(false)` | `grep -q 'insuranceDialogVisible\|createInsurance' view/src/views/other-assets/OtherAssetsView.vue` |
| 5 | Insurance details expand: `store.fetchInsuranceDetails(insuranceId)` on expand. Render `store.insuranceDetails` as nested table. CRUD: `createInsuranceDetail / updateInsuranceDetail / deleteInsuranceDetail`. | `view/src/views/other-assets/OtherAssetsView.vue` | Same pattern as estate details | `grep -q 'fetchInsuranceDetails' view/src/views/other-assets/OtherAssetsView.vue` |
| 6 | Run type-check. | — | — | `cd view && npm run type-check 2>&1 \| tail -3` |

## Notes

- Read exact `InsuranceAsset` and `InsuranceJournal` shapes from api-reference.md. Insurance API uses `has_closed` (Y/N) not `in_use`.
